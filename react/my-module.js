const hello = (name) => {
  console.log(`hello ${name}`);
};

const topla = (a, b) => a + b;
const cıkar = (a, b) => a - b;

export const text = 'text';
export const user = {
  name: 'mehmet',
  surname: 'seven',
};

export { hello, topla, cıkar };
